from custom_types import PlayerRoundHistory


def gpt4(
        round: PlayerRoundHistory
    ):
    """
    I like the idea of testing different GPT models against each other
    but it will probably be pretty $$$
    """